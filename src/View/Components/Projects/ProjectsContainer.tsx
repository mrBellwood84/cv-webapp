import { Fragment } from "react"

export const ProjectContainer = () => {
    return (
        <Fragment>
            <h3>Projects</h3>
            <p>This page should have an overview of different projects</p>
            <p>Each item must reference language and frameworks if any</p>
            <p>Each item should have a link, either or both to production page, or sourcecode on github</p>
        </Fragment>
    )
}